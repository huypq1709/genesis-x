services:
  - type: static_site
    name: genesis-x
    branch: main
    rootDir: .
    buildCommand: npm install && npm run build
    publishPath: dist
    autoDeploy: true
    pullRequestPreviewsEnabled: false
    rewrites:
      - source: /*
        destination: /index.html
        status: 200
